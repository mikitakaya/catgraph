<div class="container">
 <div class="row">
  <style>
   .field_with_errors {
    display: contents;
   }

   .login {
    margin-bottom: 15%;
   }

   .login > .field {
     margin-left: 10%;
    }

   h4 {
    margin-top: 30px;
    padding-left: 8%;
   }

   .text {
    text-align: center;
   }

   .text .field {
    margin: 15px 0;
   }

   .new_user {
    display: inline-block;
    text-align: left;
   }

   label {
    width: 200px;
   }

   .field > input, .field_with_errors > input {
    width: 350px;
   }

   .btn {
    border: 2px solid #666666;
    background-color: #999999;
    color: white;
   }

   /* タブレット縦置きサイズ（768px）以下の際に適用される設定 */
   @media screen and (max-width:768px) {
    .text {
     margin: 0 auto;
    }

    .text, .field > input, .field_with_errors > input {
     width: 330px;
    }

    .login > .field {
     margin-left: 10%;
    }

    .actions > label {
     width: 0;
     margin: 0;
    }

    .actions {
     text-align: center;
    }

    h4 {
     padding-left: 3%;
    }
   }

   /* スマートフォンサイズ（425px）以下の際に適用される設定 */
   @media screen and (max-width: 425px) {
    .text, .field > input, .field_with_errors > input {
     width: 100%;
    }

    h4 {
     padding-left: 0;
    }
   }
  </style>

  <div class="col-lg-12">
   <h4>
    <span>新規会員登録</span>
   </h4>

   <% if @user.errors.any? %>
    <%= @user.errors.count %>件のエラーが発生しました
    <ul>
     <% @user.errors.full_messages.each do |message| %>
      <li><%= message %></li>
     <% end %>
    </ul>
   <% end %>

   <div class="text">
    <%= form_with model: @user, url: user_registration_path, class: "new_user" do |f| %>
     <div class="field">
      <%= f.label :name, "名前" %>
      <%= f.text_field :name, autofocus: true, placeholder: "令和ちゃん" %>
     </div>

     <div class="field">
      <%= f.label :username, "ユーザー名" %>
      <%= f.text_field :username, placeholder: "reiwa_chan" %>
     </div>

     <div class="field">
      <%= f.label :email, "メールアドレス" %>
      <%= f.email_field :email, autocomplete: "email", placeholder: "sample@example.com" %>
     </div>

     <div class="field">
      <label for="password">
       パスワード<% if @minimum_password_length %>（<%= @minimum_password_length %>文字以上）<% end %>
      </label>
      <%= f.password_field :password, autocomplete: "new-password" %>
     </div>

     <div class="field">
      <label for="password_confirmation">パスワード（確認用）</label>
      <%= f.password_field :password_confirmation, autocomplete: "new-password" %>
     </div>

     <div class="actions", style="margin: 30px 0 50px;">
      <label></label>
      <%= f.submit "新規登録", class: "btn" %>
     </div>
     <% end %>
    </div>

    <!-- ログイン画面のリンク -->
    <div class="login">
     <h4>
      <span>
       すでに登録済みの方
      </span>
     </h4>
     <div class="field">
      <%= link_to "こちら", new_user_session_path, class: "login" %>
      からログインしてください。
     </div>
    </div>

  </div>
 </div>
</div>